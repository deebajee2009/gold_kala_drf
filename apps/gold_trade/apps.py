from django.apps import AppConfig

class GoldTradeConfig(AppConfig):
    name = 'gold_trade'  # The name of the app, in dot notation
    verbose_name = 'Gold Trade'  # A human-readable name for the app